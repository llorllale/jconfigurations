sudo: false
language: java
services:
- docker
cache:
  directories:
  - $HOME/.cache
  - $HOME/.m2
dist: trusty
jdk:
  - openjdk8

script: 
  - mvn -P release-profile clean install
  - mvn site

deploy:
  provider: script
  skip_cleanup: true
  script: "mvn --settings settings.xml site-deploy"
  on:
    tags: true
