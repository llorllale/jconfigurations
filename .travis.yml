sudo: false
language: java
services:
- docker
cache:
  directories:
  - $HOME/.cache
  - $HOME/.m2
dist: trusty
jdk:
  - openjdk8

script: 
  - mvn -P release-profile clean install

deploy:
  provider: script
  skip_cleanup: true
  script: "mvn site-deploy -X"
  on:
    tags: true
